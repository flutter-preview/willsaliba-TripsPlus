import 'package:flutter/material.dart';

class SearchMates extends StatelessWidget {
  const SearchMates({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

